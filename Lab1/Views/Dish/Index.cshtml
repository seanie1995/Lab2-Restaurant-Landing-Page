@model List<Dish>

@* <ul class="list-group all-dishes">
    @foreach (var dish in Model)
    {
        <li class="list-group-item">
            <h5>@dish.Name</h5> PRICE: @dish.Price:- @if (@dish.Availability == true)
            {
                <span class="badge text-bg-success">Available</span>
            } else
            {
                <span class="badge text-bg-danger">Unavailable</span>
            }
            <a asp-controller="Dish" asp-action="Edit" asp-route-id=@dish.Id class="btn btn-warning">Edit</a>
        </li>
    }
</ul> *@

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Price</th>
            <th scope="col">Availability</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var dish in Model)
        {
            <tr class="table-primary">
                <th scope="row">@dish.Name</th>
                <td>@dish.Price:-</td>
                @if(dish.Availability == true)
                {
                    <td><span class="badge text-bg-success">Available</span></td>
                }else
                {
                    <td><span class="badge text-bg-danger">Unavailable</span></td>
                }
                <td><a asp-controller="Dish" asp-action="Edit" asp-route-id=@dish.Id class="btn btn-warning">Edit</a></td>
                <td>
                    <form asp-controller="Dish" asp-action="Delete" asp-route-id="@dish.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this dish?');">
                        <input type="hidden" name="id" value="@dish.Id" />
                        <button type="submit" class="btn btn-danger">Delete</button>
                   </form>
                </td>
            </tr>
        }       
    </tbody>
</table>
